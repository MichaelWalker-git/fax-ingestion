const ROOTS = {
  PROCESSING_RESULTS: '/processing-results',
};

export const paths = {
  processingResults: {
    root: ROOTS.PROCESSING_RESULTS,
    view: `${ROOTS.PROCESSING_RESULTS}/view/:id`,
  },
};
