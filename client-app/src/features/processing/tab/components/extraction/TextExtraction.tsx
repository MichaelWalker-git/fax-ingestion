import { Typography } from '@mui/material'

export default function TextExtraction() {
  return (
    <Typography variant="caption">
      Extract any text from the document, the entire text content, or apply predefined templates for structured
      extraction.
    </Typography>
  )
}
